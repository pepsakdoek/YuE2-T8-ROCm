from pathlib import Path


root = Path(__file__).resolve().parent
print("YuE2 Music T8 node installed.")
print(f"Run {root / 'install_runtime.bat'} once to download the isolated runtime and model bundle.")
